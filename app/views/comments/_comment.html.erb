<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" <?php language_attributes(); ?>
<head profile="http://gmpg.org/xfn/11">
    <style>
        .Box1 {
            margin-left:40px;
            margin-bottom:30px;
            width: 99%;
        }
    </style> 
</head> 
<body>
	<div class="Box1"><h4 class="text-center">This article has <%= pluralize(@comments.count, "Comment") %> : </h4>
		<% @comments.each do |comment| %>
		<p><h5><%= link_to avatar_for(comment.user, size: 25), current_user %>  <b><%= comment.user.email %></b></h5>
			<%= time_ago_in_words(comment.created_at) %> ago <% if current_user == comment.user %>
			<span class="btn btn-outline-dark"><%= link_to 'Edit', edit_article_comment_path(comment.article, comment) %></span>
			<span class="btn btn-outline-dark"><%= link_to 'Delete', [comment.article, comment], method: :delete %></span>
			<% end %>
			<h5><p><%= comment.content %></p></h5></p>
			<hr>
			<% end %>
			<%= render 'comments/form' %></div>
		</body>
		</html>